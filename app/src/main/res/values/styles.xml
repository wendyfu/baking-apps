<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RecipeListTitle" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:background">@color/colorTransparentBlack35</item>
    </style>

    <style name="RecipeDetailTitle" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textColor">@color/colorBlack70</item>
        <item name="android:paddingTop">@dimen/dimen_24dp</item>
        <item name="android:paddingLeft">@dimen/dimen_24dp</item>
        <item name="android:paddingRight">@dimen/dimen_24dp</item>
    </style>

    <style name="PlayerTheme" parent="AppTheme">

    </style>
</resources>
